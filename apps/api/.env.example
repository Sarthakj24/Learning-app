# Local dev. On Render these are injected by render.yaml.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/whnow_learn?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"
WEB_ORIGIN="http://localhost:5173"
PORT="10000"
# Optional: password for the seeded super-admin (defaults to 'changeme123')
SEED_SUPERADMIN_PASSWORD="changeme123"
